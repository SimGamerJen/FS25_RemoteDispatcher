# Remote Dispatcher 0.3.0.0 — Early Alpha Release Candidate

This release candidate turns the proven cinematic remote-start prototype into a stateful testable alpha.

## Highlights

- Separate Management GUI and lightweight in-game target selector.
- Remote start/stop of prepared AutoDrive and Courseplay jobs without entering the vehicle.
- Distant-vehicle activation so automation can begin without player proximity.
- Optional HelperProfiles API v7 named-worker assignment.
- Per-save persistence keyed by FS25 vehicle unique IDs.
- Retained target restored after save reload.
- `rdStatus` diagnostics for support reports.

## RC validation completed — 2026-08-30

In-game testing has confirmed:

- automation/worker assignments survive a full FS25 restart;
- the retained cinematic target is restored correctly;
- named workers are used when available;
- a named worker already active elsewhere is rejected with no silent substitute hire;
- AUTO assignment follows the normal HelperProfiles selected-worker flow;
- Courseplay starts correctly with an RD-assigned worker;
- AutoDrive remote dispatch remains functional at distance.

## Remaining release blockers

- Review a clean `log.txt` covering load, Management open/close, target cycling, AD and CP start/stop, save/reload and a second dispatch.
- Promote HelperProfiles API v7 into a formal compatible HelperProfiles build, or explicitly bundle/document the compatible API v7 build for the first public alpha.
